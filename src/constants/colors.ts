import designTokens from "@/constants/designTokens.json";

export const colors = designTokens.colors;

export const brandColors = colors.brand;
export const textColors = colors.text;
export const surfaceColors = colors.surface;
export const borderColors = colors.border;
export const feedbackColors = colors.feedback;
